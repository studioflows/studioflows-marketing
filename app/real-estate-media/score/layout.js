export const metadata = {
  title: "Media Ops Score | Real Estate Media",
  description:
    "A short check for real estate media teams. Mostly tap answers. One optional messy answer.",
};

export default function MediaOpsScoreLayout({ children }) {
  return children;
}
